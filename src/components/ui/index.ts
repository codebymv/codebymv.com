// Re-export components and their types
export { default as LazyImage } from './LazyImage';
export { ProjectCarousel, type Project } from './ProjectCarousel';
export { ProjectHero } from './ProjectHero';

// Re-export icons directly from lucide-react
export { 
  ChevronLeft, 
  ChevronRight, 
  ChevronDown, 
  ExternalLink, 
  Github 
} from 'lucide-react';

// Re-export motion components from framer-motion
export { motion, AnimatePresence } from 'framer-motion';
